// createRequest.ts

import { RequestOptions } from "../interface";

const createRequest = async (options: RequestOptions = { url: '' }): Promise<any> => {

  return {};
};

export default createRequest;
